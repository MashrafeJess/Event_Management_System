@page
@model WebApp.Pages.EventInfo.PackageListModel
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
<a class="btn btn-primary mb-3 mt-5 " asp-area="" asp-page="/EventInfo/Package">Add New Package</a>

<table class="table table-bordered table-hover">
    <thead class="table-dark">
        <tr>
            <th>Sl.</th>
            <th>Package Name</th>
            <th>Creator</th>
            <th>Created Date</th>
            <th>Editor</th>
            <th>Updated Date</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.List != null && Model.List.Any())
        {
            foreach (var item in Model.List)
            {
                <tr>
                    <td>@item.PackageId</td>
                    <td>@item.PackageName</td>
					<td>@item.CreatedByName</td>
                    <td>@item.CreatedDate</td>
                    <td>@item.UpdatedByName</td>
					<td>@item.UpdatedDate</td>
                    <td>
                        <a class="btn btn-sm btn-outline-primary" asp-page="/EventInfo/Package" asp-route-Id="@item.PackageId">
                            @* <i class="bi bi-pencil-square"></i> Edit *@
                            <i class="bi bi-pen"></i>
                        </a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="5" class="text-center">No Packages available.</td>
            </tr>
        }
    </tbody>
</table>
